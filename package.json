{
  "name": "hw-w13-nodeexpresshandlebars-burger",
  "version": "1.0.0",
  "description": "In this assignment, you'll create a burger logger with MySQL, Node, Express, Handlebars and a homemade ORM (yum!). Be sure to follow the MVC design pattern; use Node and MySQL to query and route data in your app, and Handlebars to generate your HTML.\r ### Before You Begin\r * Eat-Da-Burger! is a restaurant app that lets users input the names of burgers they'd like to eat.\r * Whenever a user submits a burger's name, your app will display the burger on the left side of the page -- waiting to be devoured.\r * Each burger in the waiting area also has a `Devour it!` button. When the user clicks it, the burger will move to the right side of the page.\r * Your app will store every burger in a database, whether devoured or not.\r ## Instructions\r #### App Setup\r 1. Create a GitHub repo called `burger` and clone it to your computer.\r 2. Make a package.json file by running `npm init` from the command line.\r 3. Install the Express npm package: `npm install express`.\r 4. Create a server.js file.\r 5. Install the Handlebars npm package: `npm install express-handlebars`.\r 6. Install MySQL npm package: `npm install mysql`.\r 7. Require the following npm packages inside of the server.js file:\r    * express\r #### DB Setup\r 1. Inside your `burger` directory, create a folder named `db`.\r 2. In the `db` folder, create a file named `schema.sql`. Write SQL queries this file that do the following:\r    * Create the `burgers_db`.\r    * Switch to or use the `burgers_db`.\r    * Create a `burgers` table with these fields:\r      * **id**: an auto incrementing int that serves as the primary key.\r      * **burger_name**: a string.\r      * **devoured**: a boolean.\r 3. Still in the `db` folder, create a `seeds.sql` file. In this file, write insert queries to populate the `burgers` table with at least three entries.\r 4. Run the `schema.sql` and `seeds.sql` files into the mysql server from the command line\r 5. Now you're going to run these SQL files.\r    * Make sure you're in the `db` folder of your app.\r    * Start MySQL command line tool and login: `mysql -u root -p`.\r    * With the `mysql>` command line tool running, enter the command `source schema.sql`. This will run your schema file and all of the queries in it -- in other words, you'll be creating your database.\r    * Now insert the entries you defined in `seeds.sql` by running the file: `source seeds.sql`.\r    * Close out of the MySQL command line tool: `exit`.\r #### Config Setup\r 1. Inside your `burger` directory, create a folder named `config`.\r 2. Create a `connection.js` file inside `config` directory.\r    * Inside the `connection.js` file, setup the code to connect Node to MySQL.\r    * Export the connection.\r 3. Create an `orm.js` file inside `config` directory.\r    * Import (require) `connection.js` into `orm.js`\r    * In the `orm.js` file, create the methods that will execute the necessary MySQL commands in the controllers. These are the methods you will need to use in order to retrieve and store data in your database.\r      * `selectAll()`\r      * `insertOne()`\r      * `updateOne()`\r    * Export the ORM object in `module.exports`.\r #### Model setup\r * Inside your `burger` directory, create a folder named `models`.\r   * In `models`, make a `burger.js` file.\r     * Inside `burger.js`, import `orm.js` into `burger.js`\r     * Also inside `burger.js`, create the code that will call the ORM functions using burger specific input for the ORM.\r     * Export at the end of the `burger.js` file.\r #### Controller setup\r 1. Inside your `burger` directory, create a folder named `controllers`.\r 2. In `controllers`, create the `burgers_controller.js` file.\r 3. Inside the `burgers_controller.js` file, import the following:\r    * Express\r    * `burger.js`\r 4. Create the `router` for the app, and export the `router` at the end of your file.\r #### View setup\r 1. Inside your `burger` directory, create a folder named `views`.\r    * Create the `index.handlebars` file inside `views` directory.\r    * Create the `layouts` directory inside `views` directory.\r      * Create the `main.handlebars` file inside `layouts` directory.\r      * Setup the `main.handlebars` file so it's able to be used by Handlebars.\r      * Setup the `index.handlebars` to have the template that Handlebars can render onto.\r      * Create a button in `index.handlebars` that will submit the user input into the database.\r #### Directory structure\r All the recommended files and directories from the steps above should look like the following structure:\r ```\r .\r ├── config\r │   ├── connection.js\r │   └── orm.js\r │ \r ├── controllers\r │   └── burgers_controller.js\r │\r ├── db\r │   ├── schema.sql\r │   └── seeds.sql\r │\r ├── models\r │   └── burger.js\r │ \r ├── node_modules\r │ \r ├── package.json\r │\r ├── public\r │   └── assets\r │       ├── css\r │       │   └── burger_style.css\r │       └── img\r │           └── burger.png\r │   \r │\r ├── server.js\r │\r └── views\r     ├── index.handlebars\r     └── layouts\r         └── main.handlebars\r ```",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jccarlson27/HW-W13-NodeExpressHandlebars-Burger.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jccarlson27/HW-W13-NodeExpressHandlebars-Burger/issues"
  },
  "homepage": "https://github.com/jccarlson27/HW-W13-NodeExpressHandlebars-Burger#readme",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "express-handlebars": "^3.1.0",
    "mysql": "^2.18.1"
  }
}
